// Closed rehearsal rule. Use storage.rules.auth.example for production.
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /sessions/{sessionId}/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
